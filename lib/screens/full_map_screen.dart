import 'package:flutter/material.dart';

class FullMapScreen extends StatelessWidget {
   
  const FullMapScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('FullMapScreen'),
      ),
    );
  }
}